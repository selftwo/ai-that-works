# Agent Navigation

This is a local navigation note for agents using this cloned repo as research context.

It does not replace upstream project instructions. No build, test, release, or contribution workflow is defined here. Treat the repo as a source corpus unless the user explicitly asks for code changes.

Before scanning broadly, read:

- `AGENT_NAVIGATION_INDEX.md`

That file contains the Read Review-oriented map produced from the Phase 1 and Phase 2 scans. It points to the episodes, transcripts, specs, SOPs, and eval files that were most useful for document review, human-agent loops, provenance, reviewer personas, async state, and comment quality.

